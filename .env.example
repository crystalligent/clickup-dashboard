# ─── ClickUp Configuration ─────────────────────────────────────────────────────
# Your ClickUp API token
CLICKUP_KEY=pk_270739823_X5BBT5TZCSFD3OQJQX0GT5XVHJSS13DY

# ClickUp list ID where new tasks are created (and searched for existing tasks)
CLICKUP_LIST_ID=901611289144

# Default assignee ClickUp member ID (for QA milestone tasks, optional)
DEFAULT_ASSIGNEE_ID=89084186

# ─── GitLab Configuration ──────────────────────────────────────────────────────
# Secret token set in GitLab webhook settings (for validating requests)
GITLAB_WEBHOOK_SECRET=core-ripple

# GitLab instance URL (no trailing slash)
GITLAB_URL=https://tools.iripple.com

# GitLab personal access token (needs read_api scope for reading issues)
GITLAB_TOKEN=nr3p5pk5xsTyxDzewcum

# GitLab project ID to poll for scheduled sync
GITLAB_PROJECT_ID=394

# ─── Sync Configuration ───────────────────────────────────────────────────────
# Comma-separated milestone IDs to process (for webhook filtering)
ALLOWED_MILESTONES=802,752,756

# Milestone ID that triggers "pending review (qa)" status
QA_MILESTONE_ID=756

# Comma-separated milestone NAMES for the scheduled sync to pull from GitLab
GITLAB_MILESTONES=For review,Ongoing
